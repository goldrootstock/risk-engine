# 모델 문서 — Portfolio Risk Engine (P1-Risk), SR 11-7 구조

- 버전 1.0 · 2026-09-13 · 저자 Juwon Kim (AI 보조: Claude Code, CLAUDE.md §5) · 코드 `risk-engine` main
- 대상 모델: 1일 ES 97.5 % / VaR 99 % (FHS·EWMA), 보조: Parametric·Monte Carlo, 스트레스
- 대상 포트폴리오: `MAIN` (임의 구성, §3-4) · 정식 표본 집합 `from_1999` (§3-2)
- 모든 수치는 `risk_runs`·`backtest_results`·`stress_results` 에서 SQL 로 읽은 값이다(재계산 없음, 노트 00 #9). 파라미터 근거는 `docs/decisions.md`, 코드 설명은 `docs/walkthrough.md`.

---

## 0. 논지

**백테스트는 전부 통과했다. 그런데 이 모델은 신뢰할 만한가.** 이 문서는 통과 근거(§6-1)를 먼저 보이고, 그 다음 통과가 말해 주지 않는 것 네 가지를 본문으로 다룬다. 최악 초과가 VaR 의 6.39배였다는 것(§7-1), 집계 단위를 달력일에서 영업일로 바꾸자 구조적 결함이 드러났다는 것(§7-2), 검정력이 없는 검정이 있었다는 것(§7-3), 데이터와 포트폴리오에 알려진 한계가 있다는 것(§7-4). 결론(§8)은 "빈도 기준으로는 통과하나 꼬리 크기·다일 지평·선형성 가정에 한계가 있고, ES 와 스트레스로 보완한다" 이다.

## 1. 목적과 범위

| 항목 | 내용 |
|---|---|
| 목적 | 다자산(금리·FX·에너지) 포트폴리오의 1일 시장리스크 측정과 규제 방식 검증. 이력서 증거용 독립 프로젝트(Gap plan v2 P1) |
| 산출 | 포트폴리오 VaR 99 %, ES 97.5 %, 스트레스 ES(250일 최악 창), 상품별 컴포넌트 ES, 스트레스 시나리오 손실 |
| 범위 밖 | 옵션·비선형 상품, 신용·유동성 리스크, 실제 거래 손익(포지션은 스냅샷), 마진(P1-Margin, 별도) |
| 사용자 | 저자(학습·면접), 코드 리뷰어 |

## 2. 방법론

### 2-1. 팩터와 변화량 (노트 04)

31 계열(정식 집합 29): 미 국채 CMT(Constant Maturity Treasury, 상수만기 국채) 11 만기, ECB 환율 12 통화, EIA 에너지 현물 8. FX 는 "1 외화 = x USD" 로 재정의(S_CCY = EURUSD / EURCCY)해 로그 변화, 에너지는 USD 절대 변화(WTI 2020-04-20 = −36.98 때문에 % 불가), 국채는 bp 차분. P&L 매핑: FX q·S·(e^x−1), 에너지 q·x, 국채 −DV01·x, DV01 은 par 채권 수정 듀레이션을 매일 y 로 재계산(컨벡시티 제외, 1일 지평에서 0.5 % 미만).

### 2-2. FHS (노트 05)

EWMA λ = 0.94 [RiskMetrics 1996] 로 계열별 σ_t 를 만들고 z_t = x_t/σ_t. 최근 500 영업일의 z 벡터를 **날짜별로 함께** 뽑아 오늘 σ_T 로 되돌린 500 시나리오를 오늘 포지션에 매핑, 손실 분포에서 VaR 99 %(5 번째 손실)와 ES 97.5 %(Acerbi–Tasche 분수 가중, 12.5 개)를 읽는다. 워밍업 75 일(99 % 가중 = 74 일)은 풀·백테스트에서 제외. 스트레스 ES 는 현재 포지션으로 250 일 창을 전부 훑어 최대 [FRTB MAR33.5]. 잔차를 자산별로 섞지 않는 이유(종속성 보존)와 세 임의성의 선택지는 노트 05 §2.

### 2-3. 보조 방법과 교차검증

Parametric(델타-정규, EWMA 공분산)과 MC(정규 10,000 경로, seed 고정)는 같은 X·같은 매핑·같은 손실 정의를 쓴다. 따라서 Parametric–MC 차이 = 비선형성, MC–FHS 차이 = 분포 가정이다.

| 교차검증 | 결과 (2026-09-09, PV 177.3 M) | 판정 |
|---|---|---|
| Parametric ≈ MC (선형 장부 + 정규 충격) | VaR 883,197 vs 886,626 · ES 887,546 vs 892,987 | 일치(0.4 %·0.6 %) — 매핑·공분산 구현이 서로 검증 |
| Parametric ES/VaR = 정규 이론값 | 1.005 (이론 φ(z_{.975})/0.025 / z_{.99} = 2.338/2.326 = 1.005) | 정확 |
| FHS ES/VaR | 1.038 | 두꺼운 꼬리 — 경험분포 반영 |
| FHS 대 정규 | FHS ES 1,135,936 vs MC 892,987 (+27 %) | 꼬리 두께의 크기 |
| 컴포넌트 ES 합 = ES | 차이 < 10⁻⁹ (테스트) | Euler 분해 정확 |
| 스트레스 `wti_negative_2020` 손실 = 백테스트 최악 초과 손실 | 10,790,530 = 10,790,530 (원충격, 오늘 포지션) | 두 모듈이 같은 매핑 |
| 같은 날의 배수 | 스트레스 9.50 × ES(오늘) vs 백테스트 6.39 × VaR(당일) | 정합 — ES > VaR 이고 당일 σ 가 오늘보다 높았다 |
| FRED vs 재무부 국채 | 11 계열 겹치는 전 날짜 불일치 0 (96,134 행) | 데이터 교차검증 |

## 3. 데이터

### 3-1. 소스와 적재

| 소스 | 계열 | 기간 | 약관 | 비고 |
|---|---|---|---|---|
| FRED (연준 H.15) | DGS1MO…DGS30 11 | 1962~ | 공공저작물 (Public Domain: Citation Requested) | T+1 반영. §7-4 의 DGS30 추정 구간 |
| ECB | 유로 참조환율 12 통화 | 1999-01-04~ | 출처 표기 조건 재배포 허용 | 16:00 CET 고시 |
| EIA | 에너지 현물 8 | 1986~ | 공공저작물 | 일중 평가. §7-4 의 2005-09 공백 |

ETL 은 원본을 그대로 적재하고(결측 보정 없음), 검증은 읽기 전용이며(노트 00), 모든 적재가 `etl_runs` 에 기록된다. 총 306,431 행(2026-09-13). 원본 데이터는 저장소에 없다.

### 3-2. 표본 집합

| 집합 | 용도 | 구성 | 관측(교집합) |
|---|---|---|---|
| `from_1999` | **백테스트 정식** | 29 계열(EURCNY·UST_1M 제외), 1999-01-04~ | 6,755 (FHS 실행 6,180: 2001-04-02~2026-09-09) |
| `default` | 팩터 분석 | 31 계열, 2006-02-09~ | 5,031 |

달력 정렬은 교집합이다. `default` 에서 버려진 301 일의 구성: 미국 공휴일(EIA+FRED) 134, EIA 만 64, FRED 만 39, ECB+EIA(성금요일) 28, TARGET 휴일 28, 기타 8. 채우기(ffill)는 인공 0 변화가 독립성 검정을 오염시키므로 쓰지 않는다.

### 3-3. 데이터 품질 사실

- **DGS30 2002-02-19~2006-02-08 (994 행)은 관측이 아니라 추정치**다. 30년물 발행 중단기에 재무부가 20년 CMT 에 배율을 적용해 추정한 값을 H.15 가 계열로 공표했다 [확인: H.15 각주]. FRED sync 가 적재했고 `from_1999` 에 포함된다. 30Y 포지션(−10 M)의 그 구간 손익은 추정 금리에 기반한다.
- **2005-09-22→10-07 의 11 영업일 공백**: Henry Hub 현물이 EIA 원본에서 2005-09-23~10-06 열흘간 부재(허리케인 리타 시기 [추정])하여 교집합이 그 구간을 버렸다. 수집 실패가 아니라 소스 공백 [확인: 캐시된 원 응답]. 이 전이는 §7-2 의 15 건 중 하나다.
- FRED T+1 과 EIA 지연으로 교집합의 최신 관측은 FX 보다 하루 이상 늦다.

### 3-4. 포트폴리오 `MAIN` — 임의 구성

16 포지션, 2026-09-09 시가 177.3 M USD. 국채 롱 130 M(액면), FX 롱·숏 36 M, 에너지 순 11 M. **ES 의 82 % 가 에너지 순포지션**(WTI 롱 200,000 bbl − Brent 숏 100,000 bbl + 휘발유 롱)에서 나온다. 명목 균등도 리스크 균등도 아니며 손으로 정했다. 값은 테스트로 고정되어 변경은 커밋에 남는다(decisions.md §2-3).

## 4. 가정과 파라미터

전부 `docs/decisions.md` 의 표(§1 ETL 14, §2 수익률 7, §3 FHS 11, §4 백테스트 8, §6 스트레스 5)에 출처·대안·민감도와 함께 있다. 값은 `config/*.toml` 에 있고, 값 고정 테스트가 잡고 있으며, 파일 sha256 이 매 실행 기록에 들어간다. 결과를 보고 바꾼 값은 없다(§7 참조).

## 5. 구현과 통제

- 검사·검증·백테스트 함수는 읽기 전용이다(노트 00). `load_levels`·백테스트 적재는 `default_transaction_read_only` 세션에서 테스트한다.
- 부호 반전(P&L→손실)은 `measures.to_loss` 한 곳.
- 성공 기록은 데이터와 같은 트랜잭션, 실패 기록은 별도 트랜잭션(ETL·리스크·백테스트 모두).
- CI: ruff·mypy·pytest(실제 PostgreSQL) 매 푸시. 테스트 106 개.
- 발견된 통제 결함과 수정: 파이프가 exit code 를 가림(pipefail), 없는 티커의 포지션 CSV 가 "16행 기록" 으로 보고됨(rowcount 검사 — 빈 포지션 VaR 이 정상으로 보일 수 있던 경로; 과거 실행 10,639 건 전부 PV 159~200 M·컴포넌트 16 개로 확인), httpx 가 요청 URL 을 로그에 찍음(키 유출), 암묵 트랜잭션. 전부 `docs/walkthrough.md`.

## 6. 검증 결과

### 6-1. 백테스트 — 통과 근거 (`from_1999`, MAIN, FHS·EWMA)

| 항목 | 값 |
|---|---|
| 일수 / 초과 / 기대(1 %) | 6,179 / **63** / 61.8 (초과율 1.02 %) |
| 250 일 창 25 | Basel **녹색 23 · 황색 2 · 적색 0** |
| Kupiec POF 기각(5 %) | 1 창 — 2025-08~2026-09, 초과 0, p = 0.025, **보수 쪽** |
| Christoffersen 독립성 기각 | **0** (최저 p = 0.076) |
| 조건부 커버리지 기각 | 0 |

황색 창: 2001-05~2002-05 (5 건, 9/11 뒤 금리 주도, 11-13·14 연속), 2016-09~2017-09 (6 건, 미 대선 11-09·10 연속). `default` 집합: 4,455 일 43 건(기대 44.6), 황색 1, 적색 0.

### 6-2. 스트레스 (as_of 2026-09-09, ES 97.5 % = 1.136 M)

| 시나리오 | 창 (정렬일) | 손실 | ES 배수 | 최악 하루 |
|---|---|---|---|---|
| rates_2022 | 2022-01-03 → 10-21 | **17,604,892** | 15.5 | 2022-06-13 1.69 M |
| wti_negative_2020 | 2020-04-17 → 04-20 | 10,790,530 | 9.5 | 같은 날 |
| lehman_2008 | 2008-09-12 → 10-10 | 4,827,004 | 4.25 | 09-23 2.44 M |
| rates_june_2022 | 2022-06-09 → 06-14 | 4,028,135 | 3.55 | 06-13 |
| oil_crash_2014 | 2014-11-26 → 2015-01-15 | 3,579,833 | 3.15 | 12-02 |
| gfc_worst_week_2008 | 2008-10-03 → 10-10 | 3,385,563 | 2.98 | 10-10 1.87 M |
| election_2016 | 2016-11-08 → 11-14 | 2,518,957 | 2.22 | 11-10 |
| taper_2013 | 2013-05-21 → 06-24 | 1,400,454 | 1.23 | 06-20 1.55 M |
| covid_2020 | 2020-02-19 → 03-23 | 250,150 | 0.22 | 03-17 1.79 M |
| covid_oil_2020 | 2020-02-19 → 04-21 | 214,205 | 0.19 | 04-20 10.79 M |
| texas_freeze_2021 | 2021-02-11 → 02-18 | −702,412 (이익) | | 02-18 **7.61 M** |
| sept11_2001 · ukraine_2022 | | −2.48 M · −2.62 M (이익) | | |
| 가상: rates +200 bp / +100 / USD +10 % / 유가 −30 % | | 7.94 M / 3.97 M / 3.55 M / 3.53 M | 6.99 / 3.49 / 3.13 / 3.11 | |
| 상관: 공동 / **독립** / 무분산 | | 1.136 M / **1.979 M** / 5.596 M | 1.00 / **1.74** / 4.93 | |

## 7. 한계와 알려진 취약점 — 통과가 말해 주지 않는 것

### 7-1. 최악 초과는 VaR 의 6.39배다 — 빈도 검정은 크기를 보지 않는다

2020-04-17→20: 손실 10.83 M, VaR 1.69 M, 귀속 WTI 11.06 M(200,000 bbl × 55.29). 그 다음 날 WTI 가 +45.89 반등해 롱 WTI 는 이익이었고 EWMA σ 가 하루 만에 급등해 이후 VaR 이 손실을 덮었다. 2020 년 3~4 월 초과는 2 건뿐이다. **FHS+EWMA 는 레짐 전환에 정확히 하루 늦고, 그 하루가 6.4배다.** Kupiec 도 Christoffersen 도 이항 검정이라 이 크기를 보지 못한다 — 63/61.8 이라는 "정확한" 빈도가 그 하루를 숨긴다. 스트레스 모듈의 필터 지연 시나리오(§6-2 `wti_negative_2020`, 오늘 ES 의 9.5배)가 이를 상시 감시하며, ES 자체의 백테스트(Acerbi–Székely)는 후속 과제다.

### 7-2. 집계 단위를 바꾸자 구조적 결함이 드러났다 — 다일 지평

초과 63 건 중 19 건이 gap > 1 달력일 뒤였다. 달력일로 보면 기준선 22.6 %(주말 포함) 대비 30.2 %, 이항 p = 0.10 — 유의하지 않다. **영업일**로 갈라 보면 다르다.

| 영업일 gap h | 전이 | 초과 | 초과율 |
|---|---|---|---|
| 1 (보통 주말 포함) | 5,839 | 48 | 0.82 % |
| **≥ 2** (연휴·교집합이 버린 날) | 340 (5.5 %) | **15** | **4.41 %**, p = 2.5×10⁻⁶ |

주말이 희석하고 있었고 정작 주말 전이는 초과율이 더 낮았다(gap 3 일: 0.38 %). 여러 영업일치 손익을 1 일 VaR 과 비교하는 5.5 % 의 전이에서 모델은 체계적으로 과소추정한다 — Basel 의 지평 일치 원칙에 어긋나는 구간이다. 대응 후보 실측(340 건): √h 스케일 → 초과 2, **h 일 직접 시뮬레이션(잔차 블록 부트스트랩) → 초과 2**, 판정 불일치 0, 기대 3.4. 두 방식의 VaR 수준은 h = 2 에서 블록이 9 % 보수적(변동성 군집). 채택은 미결(JK 결정)이며 정식 결과는 아직 raw 다. 첫 집계(달력일)에서 멈췄으면 이 결함은 없는 것으로 보고됐을 것이다.

### 7-3. 검정력이 없는 검정 — PLA

PLA 는 25 창 전부 녹색(Spearman ρ = 1.000, KS ≈ 0.01)이다. 성과가 아니다. RTPL 을 선형 델타 손익으로 정의했는데 일별 규모에서 FX 지수식의 비선형성이 무시할 정도라 HPL 과 거의 같다. FRTB 의 PLA 는 프런트오피스 가격모델과 리스크 모델의 차이를 재는 것이고 이 프로젝트에는 그 차이가 존재하지 않는다. 이 검정은 형식상 구현됐을 뿐 이 장부에서 정보가 없다.

### 7-4. 데이터와 포트폴리오의 한계

- DGS30 2002~2006 은 H.15 추정치(§3-3). 국채 30Y 숏 −10 M 의 그 구간 손익은 모형 산출물에 기반한다.
- 표본에 11 영업일 공백(2005-09, Henry Hub 소스 부재)이 있고 그 전이가 §7-2 의 15 건에 들어 있다.
- 비동기 종가(ECB 14:15 CET / H.15 NY 종가 / EIA 일중)로 계열 간 상관이 과소 추정될 수 있다.
- 포트폴리오는 임의 구성이고 에너지가 ES 의 82 % 다. 모든 수치는 이 장부의 함수이며 다른 장부에서는 §6 의 상대 크기가 달라진다.
- 상관 붕괴 시나리오에서 ES 가 줄지 않고 1.74 배 커진다(§6-2). 이 장부는 분산이 아니라 WTI–Brent 헤지에 의존한다. 2020-04 에 WTI 만 음수가 된 것이 그 실례다.
- 최대 스트레스 손실은 에너지가 아니라 금리 경로(2022 년 10 개월 누적 17.6 M, ES 의 15.5 배)다. 1 일 분포에는 없는 정보다.

## 8. 결론

빈도 기준(초과 수, Basel 존, 독립성)으로 모델은 통과한다. 그러나 (1) 꼬리의 **크기**는 빈도 검정 밖에 있고 최악 초과가 6.39 배였다, (2) **다일 지평** 전이에서 4.4 % 의 구조적 과소추정이 있으며 보정 방식은 미결이다, (3) **선형성** 가정 때문에 PLA 는 정보가 없다, (4) 데이터에 추정 구간과 공백이 있고 포트폴리오는 임의다. 이 모델은 1 일 시장리스크의 **빈도**를 잘 재는 도구로 쓰되, 꼬리 크기는 ES 와 필터 지연 시나리오로, 경로 위험은 스트레스로, 지평 문제는 §7-2 의 보정으로 보완해야 한다. 파라미터는 결과를 본 뒤 바꾸지 않았고, 바꿀 경우 이 문서의 §9 와 값 고정 테스트에 남는다.

## 9. 변경 이력

| 날짜 | 변경 | 근거 |
|---|---|---|
| 2026-09-13 | v1.0 최초 작성 — ETL·수익률·FHS·백테스트·스트레스 실행 결과 기반 | 노트 00~07 |
| 2026-09-13 | 국채 소스 재무부 CSV → FRED | WAF 우회를 공개 저장소에 두지 않음; 값 불일치 0 확인 |
| 2026-09-13 | 정식 백테스트 집합 `from_1999` (29 계열) 채택 | 표본 +37 %, 2000~2001 포함 |
| 2026-09-13 | 테스트 기대값 정정 4 건 | decisions.md §5 (각 건의 이유) |

## 부록 A. 재현

```bash
python -m risk_engine.data.etl sync
python -m risk_engine.risk load-positions config/positions_main.csv
python -m risk_engine.risk backfill --from 1999-01-05 --to 2026-09-09 --universe from_1999 --tag daily_batch
python -m risk_engine.backtest run --universe from_1999 --portfolio MAIN
python -m risk_engine.risk stress --as-of 2026-09-09 --universe from_1999 --portfolio MAIN
```

데이터 스냅샷 2026-09-13. 출처: FRED(Board of Governors H.15, retrieved from FRED), ECB(Source: ECB), EIA(Source: U.S. Energy Information Administration).
