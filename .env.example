# Copy to .env (git-ignored). docker compose and the app both read these.
POSTGRES_USER=risk
POSTGRES_PASSWORD=risk
POSTGRES_DB=risk
POSTGRES_PORT=5432
DATABASE_URL=postgresql://risk:risk@localhost:5432/risk
EIA_API_KEY=
FRED_API_KEY=
